local silicon = require("nvim-silicon")
local silicon_config = {
	theme = "Vaporlush",
	font = "RecMonoDuotone Nerd Font Propo",
	tab_width = 4,
	wslclipboard = "always",
}
silicon.setup(silicon_config)
